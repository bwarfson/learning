var assert = require('assert');

describe('Reverse a String Tests', () => {
    it('should return string', () => {
        assert.equal("string", typeof(Reverse("")));
    });
});

const Reverse = value => value;